[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo_mysql", "php83Extensions.mbstring", "php83Extensions.xml", "php83Extensions.zip", "php83Extensions.gd", "php83Extensions.curl", "php83Extensions.opcache", "composer", "nodejs_20"]

[phases.build]
cmds = [
    "composer install --no-dev --optimize-autoloader --ignore-platform-reqs",
    "npm ci",
    "npm run build"
]

[phases.deploy]
cmds = [
    "php artisan config:cache",
    "php artisan route:cache",
    "php artisan migrate --force --seed"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"